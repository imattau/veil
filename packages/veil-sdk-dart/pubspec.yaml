name: veil_sdk
version: 0.1.0
publish_to: none
description: Dart/Flutter SDK scaffold for VEIL protocol.

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter_rust_bridge: ^2.11.1
  ffi: ^2.1.3
  meta: ^1.12.0
  web_socket_channel: ^2.4.5
  sqflite: ^2.3.3
  flutter_reactive_ble: ^5.1.1
  cryptography: ^2.7.0
  flutter_secure_storage: ^9.2.2

# Optional platform deps can be added by the app (sqflite, drift, flutter_reactive_ble).

dev_dependencies:
  test: ^1.25.8
